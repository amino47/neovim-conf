# AcidCat's Neovim Dots

These are my neovim dotfiles.

## Organization
Most configuration is in files in either `/plugin/` or `/lua/`. The init.lua is kept intentionaly brief.
Plugin specs are defined in individual files in `/lua/plugins`
Plugin config/setup is in the `/plugin/plugin.lua`
General configuration is in `/plugin/conf.lua`
